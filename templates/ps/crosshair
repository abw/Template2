[%  INCLUDE ps/ring  UNLESS global.ps.loaded.ring;
    INCLUDE ps/cross UNLESS global.ps.loaded.cross;
    global.ps.loaded.crosshair = 1;
-%]
/crosshair {
    /y exch def
    /x exch def
    x y ring
    x y cross
} def
